import ScreenStub from '../components/ScreenStub'

export default function SendLetter() {
  return (
    <ScreenStub
      title="Send a letter"
      note="Picks a stamp and renders it into a short video to share — the render script is coming."
    />
  )
}
