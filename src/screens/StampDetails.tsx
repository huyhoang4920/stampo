import ScreenStub from '../components/ScreenStub'

export default function StampDetails() {
  return (
    <ScreenStub
      title="Stamp details"
      note="Date and location for the cut stamp, then save it into the collection."
    />
  )
}
